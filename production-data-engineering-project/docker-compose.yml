version: "3.9"

services:

  postgres:
    image: postgres:16

    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: crypto_db

    ports:
      - "5432:5432"

  etl:
    build: .
    depends_on:
      - postgres