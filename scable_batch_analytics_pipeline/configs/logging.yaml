version: 1

formatters:
  standard:
    format: "%(asctime)s - %(levelname)s - %(message)s"

handlers:
  file:
    class: logging.FileHandler
    filename: logs/pipeline.log
    formatter: standard

root:
  level: INFO
  handlers: [file]